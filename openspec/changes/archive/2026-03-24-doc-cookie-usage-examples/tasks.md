## 1. 文档更新

- [x] 1.1 在 `README.md` 的「使用方法」流程中，于环境变量小节之后新增 **Cookie usage examples**，包含：`npx` + `--cookie` 示例、MCP `env` + `MG_MCP_COOKIE` 的 JSON 示例、占位符与安全提示。
- [x] 1.2 在 `README.zh-CN.md` 中新增对应 **Cookie 使用示例**（与英文结构一致、简体中文表述）。
- [x] 1.3 快速通读两文件，确认示例中的包名为 `magic-mcp-private`，且未出现真实 Cookie 内容。
